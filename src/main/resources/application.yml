spring:
  datasource:
    url: jdbc:h2:mem:credit_aplication_system_db
    username: bruno
    password:
  jpa:
    show-sql: true
    hibernate:
      properties:
        hibernate.format_sql: true
#      ddl-auto: create-drop
  h2:
    console:
      enabled:  true
      path: /h2-console
      settings:
        trace: false
        web-allow-others: false